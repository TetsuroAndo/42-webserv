#include "HttpProcessor.hpp"

HttpProcessor::HttpProcessor() {}

HttpProcessor::~HttpProcessor() {}

void HttpProcessor::processRequest(const HttpRequest &request) {
	// Process the incoming HTTP request
}
