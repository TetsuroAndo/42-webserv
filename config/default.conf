# 実際のNGINX風の設定例に似せてみる
server {
    listen 8080;
    server_name localhost;

    # ドキュメントルート、エラーページなど
    root /path/to/your/www;
    error_page 404 /404.html;

    # ここでlocationディレクティブやCGI設定を行うが、超ミニマルでは省略
}
