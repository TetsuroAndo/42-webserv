servers:
  - server:
      listens:
        - listen:
            interface: 0.0.0.0
            port: 8080

      maxRequestBodySize: 1KB
      timeoutSec: 60
      maxEvents: 1024

      access_logs:
        - access_log:
            disable: true

      error_logs:
        - error_log:
            disable: true

      locations:
        # Location for testing uploads and deletes specifically
        - location:
            path: /upload
            root: ./test/post_test/uploads
            uploadStore: ./test/post_test/uploads
            allowedMethods:
              - POST
              - DELETE