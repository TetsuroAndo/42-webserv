servers:
  - server:
      listens:
        - listen:
            interface: 0.0.0.0
            port: 8080
      access_logs:
        - access_log:
            sink: console
            invalid_key: true
