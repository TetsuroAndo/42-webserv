servers:
  - server:
      listens:
        - listen:
            interface: 0.0.0.0
            port: 8080
      locations:
        - location:
            # TODO: 思いつかない
            path:
            root: ./www

      error_logs:
        - error_log:
            sink: file
            level: debug
        - error_log:
            sink: console
            level: debug
