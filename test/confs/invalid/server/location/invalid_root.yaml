servers:
  - server:
      listens:
        - listen:
            interface: 0.0.0.0
            port: 8080
      locations:
        - location:
            path: /
            # TODO: 思いつかない
            root:
      error_logs:
        - error_log:
            sink: file
            level: debug
        - error_log:
            sink: console
            level: debug
