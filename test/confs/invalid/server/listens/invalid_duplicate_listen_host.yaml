servers:
  - server:
      listens:
        - listen:
            interface: 127.0.0.1
            port: 8080
            host: example.com
  - server:
      listens:
        - listen:
            interface: 127.0.0.1
            port: 8080
            host: example.com
