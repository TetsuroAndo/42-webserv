servers:
  - server:
      listens:
        - listen:
            interface: 0.0.0.0
            port: 8080
        - listen:
            interface: 127.0.0.1
            port: 3000

      locations:
        - location:
            path: /
            root: ./test/test_www/static
            allowedMethods:
              - HEAD
              - GET