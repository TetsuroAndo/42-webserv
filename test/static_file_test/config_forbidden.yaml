servers:
  - server:
      listens:
        - listen:
            interface: 0.0.0.0
            port: 8080

      locations:
        - location:
            path: /forbidden_dir/
            root: ./www/forbidden_dir
            autoindex: true
            allowedMethods:
              - GET
              - HEAD
